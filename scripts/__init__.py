"""Repo-top-level auxiliary scripts (non-agent, non-orchestrator).

Scripts live here so they can import ``orchestrator`` / ``rag`` / ``schemas``
as packages without dubbing themselves part of any. Run via
``python -m scripts.<name>`` or import programmatically.
"""
