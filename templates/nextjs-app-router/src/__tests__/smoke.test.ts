import { describe, expect, it } from "vitest";

/**
 * Smoke test — proves vitest is wired. Delete once real unit tests land.
 */
describe("smoke", () => {
  it("arithmetic still works", () => {
    expect(2 + 2).toBe(4);
  });
});
